version: "1"
packages:
  - name: "db"
    path: "internal/db"
    queries: "sql/queries"
    schema: "sql/schema"
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: false
